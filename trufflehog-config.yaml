detectors:
  - name: GenericSecret
    keywords:
      - "password"
      - "secret"
      - "api_key"
      - "aws_secret"
      - "token"
    regex:
      secret_pattern: '(?i)(password|secret|api_key|token|aws_secret)["\s]*[=:]["\s]*[A-Za-z0-9+/]{8,}'
